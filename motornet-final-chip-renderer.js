(function(){
  // Compatibility stub.
  // EV/ICE spec chips are rendered by motornet-consumption-ui.js.
  // This file intentionally does not wrap renderCarVisual, because a second
  // renderer was removing consumption/WLTP chips and leaving only battery.
  window.__motornetFinalChipRendererDisabled = true;
})();
